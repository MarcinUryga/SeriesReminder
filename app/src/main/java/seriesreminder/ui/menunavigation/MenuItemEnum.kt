package seriesreminder.ui.menunavigation

import com.mu.marci.series_reminder.R

/**
 ** Created by marci on 2018-04-02.
 */
enum class MenuItemEnum(val itemId: Int) {
  OVERVIEW(R.id.nav_series_overview),
  WATCHLIST(R.id.nav_series_watchlist),
  SETTINGS(R.id.nav_settings)
}